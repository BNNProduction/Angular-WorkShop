import { ReplaceWithUpperCaseDotPipe } from './replace-with-upper-case-dot.pipe';

describe('ReplaceWithUpperCaseDotPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWithUpperCaseDotPipe();
    expect(pipe).toBeTruthy();
  });
});
